template: Hello, %s!
defaultName: green

http:
  rootPath: /service/*  # Default is /*
  useDirectBuffers: false  # Support local live reload

# Logging settings.
logging:

    # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
    level: WARN
